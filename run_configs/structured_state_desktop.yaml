# Primary desktop path: structured accessibility state → LLM → semantic actions.
# Uses a single strong model for all steps (no routing).
adapter: "structured_state_desktop"
tasks:
  - "tasks/desktop_textedit_save/task.yaml"
max_steps: 30
trial_count: 1
