# Legacy comparison lane: OpenAI computer-use (screenshot-first) on browser tasks.
# Retained for benchmarking against the structured-state desktop path.
adapter: "openai_cu"
tasks:
  - "tasks/browser_download/task.yaml"
  - "tasks/browser_form_fill/task.yaml"
max_steps: 15
trial_count: 1
