# Legacy comparison lane: OpenAI computer-use hybrid (screenshot + ARIA) on browser tasks.
# Retained for observation-mode comparison against screenshot-only (openai_browser.yaml).
adapter: "openai_cu_hybrid"
tasks:
  - "tasks/browser_download/task.yaml"
  - "tasks/browser_form_fill/task.yaml"
max_steps: 15
trial_count: 1
